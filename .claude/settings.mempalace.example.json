{
  "_comment": "Reference shape for opting into mempalace (https://mempalaceofficial.com). Install with `pip install mempalace && mempalace init .`, then copy the mcpServers + hooks blocks into .claude/settings.json (or ~/.claude/settings.json). Check https://mempalaceofficial.com/guide/hooks and /reference/mcp-tools for the current commands — the values below may drift.",

  "mcpServers": {
    "mempalace": { "command": "mempalace", "args": ["mcp"] }
  },

  "hooks": {
    "PreCompact": [
      { "matcher": "", "hooks": [{ "type": "command", "command": "mempalace mine --mode convos --source-path \"$CLAUDE_PROJECT_DIR\" || true" }] }
    ],
    "Stop": [
      { "matcher": "", "hooks": [{ "type": "command", "command": "mempalace mine --mode convos --source-path \"$CLAUDE_PROJECT_DIR\" || true" }] }
    ]
  }
}
